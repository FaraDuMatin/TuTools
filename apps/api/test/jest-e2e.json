{
  "moduleFileExtensions": ["js", "mjs", "json", "ts"],
  "rootDir": ".",
  "testEnvironment": "node",
  "testRegex": ".e2e-spec.ts$",
  "transform": {
    "^.+\\.(t|j)s$": "ts-jest",
    "^.+\\.mjs$": ["ts-jest", { "tsconfig": { "allowJs": true } }]
  },
  "//": "better-auth and its transitive deps are ESM-only, and some (@noble/hashes) ship ESM under a .js extension, so no extension-based rule works. Node 22 runs this fine via require(esm); Jest's own module system does not, so everything gets transformed.",
  "transformIgnorePatterns": [],
  "moduleNameMapper": {
    "^(\\.{1,2}/.*)\\.js$": "$1"
  }
}
